#include <stdio.h>
int main()
{
    int n;
    printf("enter your two numbers");
    scanf("%d ", &n);
    for (int j = 1; j <= n; j++)
    {

        for (int i = 1; i <= ; i++)
        {
            printf("%d", i);
        }
        printf("\n");
    }
    return 0;
}